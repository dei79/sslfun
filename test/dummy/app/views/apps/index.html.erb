<h1>Listing apps</h1>

<table>
  <tr>
    <th>Name</th>
    <th>App cert</th>
    <th>App key</th>
    <th></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @apps.each do |app| %>
  <tr>
    <td><%= app.name %></td>
    <td><%= app.app_cert_id %></td>
    <td><%= app.app_key_id %></td>
    <td><%= link_to 'Get CSR', csr_app_path(app) %></td>
    <td><%= link_to 'Show', app %></td>
    <td><%= link_to 'Edit', edit_app_path(app) %></td>
    <td><%= link_to 'Destroy', app, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New App', new_app_path %>
